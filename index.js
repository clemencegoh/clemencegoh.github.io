"use strict";

const [projectsData, skillsData] = getData();
console.log(projectsData, skillsData);

/**
 * Components
 */
{
  function SomeComponent() {}

  // const someContainer = document.querySelector("#");
  // ReactDOM.render(React.createElement(SomeComponent), someContainer);
}
